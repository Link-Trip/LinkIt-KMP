package com.linkit.company.core.designsystem.component.badge

/**
 * [LinkItBadge] 의 색상 속성.
 *
 * - [Neutral]: 회색 계열 (기본값).
 * - [Accent]: 강조 색. 기본은 primary 이며 `accentBackgroundColor`·`accentContentColor` 로 커스텀 가능.
 */
enum class BadgeColor {
    Neutral,
    Accent,
}
